server:
  port: 9000

eureka:
  instance:
    prefer-ip-address: true
  client:
    refresh:
      enable: false
    service-url:
      defaultZone: http://localhost:8761/eureka/

app:
  message: 'Hello from Config Server 222'
  discount: 15
